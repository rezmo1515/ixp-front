import { useRef } from 'react'
export default function OtpInput({value,onChange}){const refs=useRef([]);const arr=value.split('').concat(Array(6).fill('')).slice(0,6);const set=(i,v)=>{const n=arr.slice();n[i]=v;onChange(n.join('').slice(0,6))};return <div className='flex gap-2'>{arr.map((d,i)=><input key={i} ref={e=>refs.current[i]=e} value={d||''} maxLength={1} onChange={e=>{const v=e.target.value.replace(/\D/g,'');set(i,v);if(v&&refs.current[i+1])refs.current[i+1].focus()}} onKeyDown={e=>{if(e.key==='Backspace'&&!arr[i]&&refs.current[i-1])refs.current[i-1].focus()}} onPaste={e=>{const p=e.clipboardData.getData('text').replace(/\D/g,'').slice(0,6);onChange(p)}} className='w-10 h-10 text-center bg-tertiary border border-border rounded'/> )}</div>}
